{
  "id": "get_trip",
  "skill_id": "travel",
  "name": "Get Trip",
  "description": "Get one known trip by canonical trip_id. Use the canonical id from active_entities, not the trip title.",
  "status": "idea",
  "mode": "read",
  "risk_level": "low",
  "confirmation_required": false,
  "audit_required": true,
  "input_schema": {
    "type": "object",
    "properties": {
      "trip_id": {
        "type": "string",
        "description": "Trip id"
      }
    },
    "required": ["trip_id"]
  },
  "output_schema": {}
}
