{
  "id": "get_spot",
  "skill_id": "travel",
  "name": "Get Spot",
  "description": "Get one timeline item as a Spot detail record.",
  "status": "implemented",
  "mode": "read",
  "risk_level": "low",
  "confirmation_required": false,
  "audit_required": true,
  "input_schema": {
    "type": "object",
    "properties": {
      "timeline_item_id": {
        "type": "string",
        "description": "Travel timeline item id"
      }
    },
    "required": ["timeline_item_id"]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "timeline_item_id": {
        "type": "string"
      },
      "spot": {
        "type": ["object", "null"]
      },
      "source": {
        "type": "string",
        "const": "local_travel_read"
      }
    },
    "required": ["timeline_item_id", "spot", "source"]
  }
}
